/**
 * RevenueCat public API key — template.
 *
 * Copy this file to rc-config.js (same folder) and fill in the real
 * production key before building for release. rc-config.js is
 * gitignored on purpose: the production key must never be committed.
 *
 * Get the key from: RevenueCat dashboard -> Project Settings -> API Keys
 * -> Apple App Store (the public SDK key, starts with "appl_").
 */
window.RC_API_KEY = 'REPLACE_ME';
